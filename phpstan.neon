includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    paths:
        - src
        - config
        # Uses the StreamsChanges trait, so PHPStan analyses it.
        - tests/Fixtures/Models
    tmpDir: .phpstan
    noEnvCallsOutsideOfConfig: false
